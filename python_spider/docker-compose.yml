 version: '3.8'
 services:
  splash: 
    image: scrapinghub/splash:latest
    ports:
      - 8050
  app: 
     image: my:con
     build: a2.Dockerfile
    #  environment:
    #    NODE_ENV: development
    #  ports:
    #    - 3000
    #    - 9229:9229
    #  command: node --inspect=0.0.0.0:9229 ./bin/www

#   web:
#     build: .
#     ports:
#     - "5000:5000"
#     volumes:
#     - .:/code
#     - logvolume01:/var/log
#     links:
#     - redis
#   redis:
#     image: redis
# volumes:
#   logvolume01: {}